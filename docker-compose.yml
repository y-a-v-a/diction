version: '3.8'

services:
  diction:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diction-app
    ports:
      - "3000:3000"
    environment:
      # Anthropic API Key - REQUIRED
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

      # TTS Service Configuration
      - TTS_SERVICE=${TTS_SERVICE:-elevenlabs}

      # ElevenLabs Configuration (if using elevenlabs)
      - ELEVENLABS_API_KEY=${ELEVENLABS_API_KEY}
      - ELEVENLABS_VOICE_ID=${ELEVENLABS_VOICE_ID}

      # Resemble.ai Configuration (if using resemble)
      - RESEMBLE_API_KEY=${RESEMBLE_API_KEY}
      - RESEMBLE_VOICE_UUID=${RESEMBLE_VOICE_UUID}

      # Secret token for /create route access
      - CREATE_SECRET_TOKEN=${CREATE_SECRET_TOKEN}

      # Server configuration
      - PORT=3000
      - NODE_ENV=production

    restart: unless-stopped

    # Health check
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3

    # Security options
    security_opt:
      - no-new-privileges:true

    # Resource limits (optional, adjust as needed)
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M
